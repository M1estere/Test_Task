using UnityEngine;

public class PunchCollider : MonoBehaviour
{
    /*[SerializeField] private Animator _punchAnimator;

    private void Awake()
    {
        _punchAnimator.enabled = false;
    }

    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.TryGetComponent(out SonicController controller))
        {
            controller.SetPunchable(_punchAnimator);
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.gameObject.TryGetComponent(out SonicController controller))
        {
            controller.SetUnpunchable();
        }
    }*/
}
